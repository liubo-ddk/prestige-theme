<section id="section-{{ section.id }}" data-section-id="{{ section.id }}" data-section-type="image-slider">
  <div class="ImageSlider">
    <div class="ImageSlider__Wrapper">
      {%- for block in section.blocks -%}
        <div class="ImageSlider__Card">
          <a href="{{ block.settings.link }}">
            <img src="{{ block.settings.image }}" alt="{{ block.settings.text }}">
            <div class="ImageSlider__Text">{{ block.settings.text }}</div>
          </a>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

<style>
  #section-{{ section.id }} {
    overflow-x: scroll;
    white-space: nowrap;
  }
  .ImageSlider__Wrapper {
    display: flex;
  }
  .ImageSlider__Card {
    display: inline-block;
    margin-right: 10px;
    text-align: center;
  }
  .ImageSlider__Card img {
    max-width: 100%;
    height: auto;
    display: block;
    margin: 0 auto;
  }
  .ImageSlider__Text {
    margin-top: 5px;
    color: #333;
  }
  .ImageSlider__Card a {
    text-decoration: none;
    color: inherit;
  }
  .ImageSlider__Card a:hover .ImageSlider__Text {
    text-decoration: underline;
  }
</style>

{% schema %}
{
  "name": "Vertical Image Slider",
  "settings": [],
  "blocks": [
    {
      "type": "image_card",
      "name": "Image Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Vertical Image Slider"
    }
  ]
}
{% endschema %}